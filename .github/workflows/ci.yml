on: push
name: CI

jobs:
  pylint:
    name: Pylint
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    - name: Pylint
      uses: cclauss/GitHub-Action-for-pylint@master
      with:
        args: "pip install -r requirements.txt ; pylint -d E0401 src/*.py src/lib/*.py"
